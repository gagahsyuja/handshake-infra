apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: handshake-production
  labels:
    app: handshake
    environment: production
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/gagahsyuja/handshake-infra.git
    targetRevision: HEAD
    path: charts/production
    helm:
      valueFiles:
        - /values/values-production.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: handshake-production
  syncPolicy:
    automated:
      enabled: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
