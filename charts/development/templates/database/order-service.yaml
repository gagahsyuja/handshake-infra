apiVersion: v1
kind: Service
metadata:
  name: {{ template "handshake.order.db" . }}
  labels: {{ include "handshake.order.db.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace | quote }}
spec:
  type: ClusterIP
  selector: {{ include "handshake.order.db.labels" . | nindent 4 }}
  ports:
    - protocol: TCP
      port: {{ include "handshake.order.db.port" . | nindent 8 }}
      targetPort: {{ include "handshake.order.db.port" . | nindent 8 }}
