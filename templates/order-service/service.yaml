apiVersion: v1
kind: Service
metadata:
  name: {{ template "handshake.order" . }}
  labels: {{ include "handshake.order.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace | quote }}
spec:
  type: ClusterIP
  selector: {{ include "handshake.order.labels" . | nindent 4 }}
  ports:
    - protocol: TCP
      port: {{ template "handshake.order.port" . }}
      targetPort: {{ template "handshake.order.port" . }}
