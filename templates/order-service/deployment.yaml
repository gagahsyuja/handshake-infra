apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "handshake.order" . }}
  labels: {{ include "handshake.order.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace | quote }}
spec:
  replicas: {{ .Values.orderService.replicas }}
  selector:
    matchLabels: {{ include "handshake.order.labels" . | nindent 6 }}
  template:
    metadata:
      labels: {{ include "handshake.order.labels" . | nindent 8 }}
    spec:
      containers:
        - name: order-service
          image: {{ .Values.orderService.image.repository }}:{{ .Values.orderService.image.tag }}
          ports:
            - containerPort: {{ template "handshake.order.port" . }}
          envFrom:
            - secretRef:
                name: {{ .Values.orderService.secretName }}
          livenessProbe:
            httpGet:
              path: /live
              port: {{ template "handshake.order.port" . }}
            initialDelaySeconds: 5
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /ready
              port: {{ template "handshake.order.port" . }}
            initialDelaySeconds: 5
            periodSeconds: 5
