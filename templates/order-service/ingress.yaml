apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ template "handshake.order" . }}
  labels: {{ include "handshake.order.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace | quote }}
spec:
  ingressClassName: nginx
  rules:
    - host: {{ template "handshake.order.host" . }}
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: {{ template "handshake.order" . }}
                port:
                  number: {{ template "handshake.order.port" . }}
